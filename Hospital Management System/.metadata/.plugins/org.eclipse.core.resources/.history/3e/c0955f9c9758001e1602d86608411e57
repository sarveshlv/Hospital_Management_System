package com.hms.bookingms.service;

import com.hms.bookingms.dto.AddBookingRequest;
import com.hms.bookingms.entities.Booking;
import com.hms.bookingms.exception.PatientNotFoundException;
import com.hms.bookingms.exceptions.BookingNotFoundException;

public interface IBookingService {
	Booking addBooking(AddBookingRequest addBookingRequest) throws PatientNotFoundException;
	Booking getBookingByPatientId(String patientId) throws PatientNotFoundException;
	Booking approveBooking(String bookingId) throws BookingNotFoundException;
	Booking rejectBooking(String bookingId);
	Booking cancelBooking(String bookingId);
	Booking checkBooking(String bookingId);
}